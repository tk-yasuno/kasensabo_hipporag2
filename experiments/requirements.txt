# ──────────────────────────────────────────────
# RAG 比較実験 requirements.txt
# Python 3.12 / .venv-hipp / GPU 16GB 環境
#
# インストール:
#   .\.venv-hipp\Scripts\pip install -r experiments/requirements.txt
# ──────────────────────────────────────────────

# ── RAG / 検索 ────────────────────────────────
rank-bm25>=0.2.2                # BM25Okapi
faiss-cpu>=1.8.0                # IndexFlatIP（VRAM 不要）
sentence-transformers>=3.0.0    # hotchpotch/static-embedding-japanese

# ── Ollama API クライアント ────────────────────
httpx>=0.27.0                   # 非同期 HTTP（Timeout 設定付き）

# ── データ処理 ────────────────────────────────
numpy>=1.26.0
tqdm>=4.66.0

# ── 集計・可視化 ──────────────────────────────
pandas>=2.2.0
matplotlib>=3.9.0
japanize-matplotlib>=1.1.3      # 日本語フォント（なければ Meiryo フォールバック）
